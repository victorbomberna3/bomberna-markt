# analysis

Python layer. Empty until DECISIONS.md Q1 is answered.

If **Path A** (recommended start): a small project that reads the CSV exported from the
frontend and produces season reporting with pandas (revenue by weekday, by month, Easter
window, averages, staff vs revenue) plus charts.

If **Path B**: this becomes a FastAPI service with a database and a sync API for the phone.

Do not build until the path is chosen.
